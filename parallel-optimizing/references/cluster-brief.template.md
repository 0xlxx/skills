# 簇简报: <簇名>（<优化目标>，<SSOT 版本>）

> 先读同目录 _COMMON.md（模板/方法/铁律/退出条件），再读本文件。本文件只含本簇特化信息。

## 0. OPTIMIZATION_CLASSES.tsv 行

<模块 | 热点占比 | 优化类型 | 顺序相关性 | 共享状态风险 | 验证命令>

## 1. 热点证据（先量化，再动手）

- --phase 占比: <如 P3 sweep 30-57%，large 下 57%>
- --cpu 函数热点: <函数名 + 占比/样本>
- 复杂度线索: n/2n/4n 缩放 <线性/超线性>

## 2. 优化假设

- 改什么: <文件/函数 + 改法>
- 为什么快: <机制: 免分配? typed array? 缓存? 算法重排?>
- 预期收益: <≥20%? 依据>

## 3. 顺序相关性分类（bit-exact 红线判定）

- 本簇是否改变遍历/求和/排序顺序: <是 → 非 bit-exact 通道, 需显式豁免授权; 否 → 应保持 bit-exact>
- 涉及的顺序相关归约点: <逐点列出, 如 barycenter 按端口序累加>
- 涉及共享可变状态: <缓存返回可变对象? 失效点枚举?>

## 4. Scope 边界（本 worktree 内可碰）

| 文件 | 改动 |
|---|---|
<目标模块>** | 主体优化 |
<共享图模型> | 只加缓存/免分配视图, 结构变更唯一入口走 setter |
<verify 脚本> | 不改（主线程管门禁） |
<登记文档> | 完成后回写 OPTIMIZATION_CLASSES.tsv |

**不可碰**：其余一切文件（尤其易冲突模块、现有测试）。

## 5. 方法 + 退出条件（机器可检查）

```bash
<bit-exact 命令> '<簇 glob>'   # bit-exact 覆盖不降
<热点定位命令> '<簇规模>'    # 目标模块耗时对比
<端到端 bench>  # 端到端
<对齐命令>  # 全量对齐
<回归命令>
```

退出条件: bench 目标模块提升 ≥ max(20%, 3×CV) + 其余不降 + bit-exact 覆盖不降 + 696 0 失败。
未满足不得声称完成。
