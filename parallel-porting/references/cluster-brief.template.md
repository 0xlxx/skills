# 簇简报: <簇名>（<任务>，<SSOT 版本> → <目标>）

> 先读同目录 _COMMON.md（模板/方法/铁律/退出条件），再读本文件。本文件只含本簇特化信息。

## 1. 目标

<一句话：移植/修复什么，SSOT 主类，规模（文件数/行数）>

## 2. 源模块清单（ast-grep outline 逐符号对比，全部覆盖）

<文件/类列表，按阶段分组>

## 3. 触发语料与 oracle（无触发的算法必须自建）

- 新建 <语料路径>（如 test-resources/elk-models/tickets/core/9XX_*.elkt）
- **算法必须写在 CHILD 节点**（root 算法可能被 harness 强制覆盖，测不到目标；参考已移植先例）
- 生成 oracle：<oracle 生成命令>
- 簇级差分：<verify 命令> → 全等（ε=1e-3）

## 4. Scope 边界（本 worktree 内可碰）

| 文件 | 改动 |
|---|---|
| <目标模块路径>** | 新建/修改（主体） |
| <共享分派文件> | 只加 <N> 处：集合 + switch case + 过滤；其余不动 |
| <共享 verify 脚本> | isLayered/可移植判断加 <算法> 集合 |
| <语料路径> | 新建 |
| <BDD 测试路径> | 新建（算法不变量） |
| <登记文档> | 登记完成 |

**不可碰**：其余一切文件（尤其 <易冲突模块>、现有测试）。

## 5. 关键验证命令（在 <工作目录> 下）

```bash
<oracle 生成>       # 生成 jar/参考 oracle
<簇级 verify>       # 簇级差分（全等）
<单测/bdd>          # bdd 全过
<回归 collect-all && check>  # 回归 0 失败
<oracle 测试>       # oracle 全过
<全量 verify>       # 全量不降（≥基线）
```

## 6. 提示

- 需要源语言语义辅助（随机数/哈希序/位压缩）时，先查 <目标 util 目录> 是否已移植，复用。
- 若依赖共享算法（compaction/spacing），先查 <共享模块> 是否已移植。
- 完成后按 _COMMON.md 汇报格式汇报 + 提交。
