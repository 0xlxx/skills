name: check-secrets
on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: 安全门禁 — 真实凭据/IP/令牌必须 0 命中
        run: bash scripts/check-secrets.sh
